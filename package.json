{
  "name": "finder-anywhere",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build-info": "node scripts/update-build-info.js",
    "dev": "yarn run build-info && tauri dev",
    "build": "tauri build",
    "dev:ui": "vite --host 127.0.0.1 --port 1420",
    "build:ui": "vite build",
    "preview": "vite preview",
    "logo": "tauri icon ./logo.png",
    "sidecar": "node scripts/copy-sidecar.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "aplayer": "^1.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.0.0"
  }
}
